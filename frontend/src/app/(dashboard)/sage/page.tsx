import SageIntegrationClient from "./SageIntegrationClient";

export const metadata = { title: "Sage Integration | BizPilot" };

export default function SagePage() {
  return <SageIntegrationClient />;
}
