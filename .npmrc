# Hoist React packages to avoid duplicate instances
public-hoist-pattern[]=react
public-hoist-pattern[]=react-dom
public-hoist-pattern[]=@radix-ui/react-*

# Strict peer dependencies
strict-peer-dependencies=true
